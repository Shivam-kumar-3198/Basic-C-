// finding the error in this code
/*
#include<iostream>
using namespace std;
int main(){
    int size;
    cin>>size;
    int arr[size];
    for( i=1; i<=size; i++ ){  //here int i is not declared
        cin>>arr[i];           //indexing of array will always be n-1 when it goes to 3 it will return error
        cout<<arr[i];
    }
    return 0;
}
*/