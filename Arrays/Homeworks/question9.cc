// Count the number of elements in given array greater than a given number x
#include<iostream>
using namespace std;
int main(){
    int arr[] = {1,3,0,10,2,5,6};
    int x = 4;
    int n = sizeof(arr) / sizeof(arr[0]);
    
    int count = count_if(arr, arr + n, [x](int num{}))


}
