#include<iostream>
using namespace std;
int main(){
    int arr[5];
    arr[0] = 6;
    arr[1] = 2;
    arr[2] = 45;
    arr[3] = 64;
    arr[4] = 74;
    arr[5] = 90;
    for(int i=0;i<=4;i++){
        cout<<arr[i]<<" ";
    }
    // We have solved the array problem using Loop
    // In the next problem we have to take input form the user

}