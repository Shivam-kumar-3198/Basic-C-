// program to count number alphabets, digits and special character

#include<iostream>
#include<string.h>

#define MAX_SIZE 100

using namespace std;

int main(){
    char string[MAX_SIZE];
    int aalphabets, digits, specialchars,i;

    alphabets = digits = specialchars =i=0;

    cout"Enter a string :";
    cin>>string;

    while(sring[i] != '/0'){
        if((string[i] >='a' && string[i] <= 'z' \\(string[i] >=)))
    }

}