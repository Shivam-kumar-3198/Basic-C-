#include <iostream>
#include <cmath> // Corrected header file

using namespace std;

int main() {
    // cout << cbrt(9);
    // return 0; // Add a return statement to indicate successful execution
     cout<<max(4,8)<<endl;
     cout<<min(4,8);
}