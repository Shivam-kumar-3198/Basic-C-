#include <iostream>
using namespace std;

int sum(int a, int b) {
    return a + b;
}

int main() {
    cout << sum(7, 90);
    return 0; // Don't forget to include a return statement in the main function
}
